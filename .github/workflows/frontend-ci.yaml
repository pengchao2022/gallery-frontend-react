name: Build and Push React Frontend to ECR

on:
  pull_request:
    types:
      - closed
    paths:
      - 'src/**'           
      - 'public/**'
      - 'Dockerfile'       
      - 'package.json'
      - 'index.html'
      - '.github/workflows/frontend-ci.yaml'  

  push:
    branches:
      - "feature/**"

    paths:
      - 'src/**'           
      - 'public/**'
      - 'Dockerfile'       
      - 'package.json'
      - 'index.html'
      - '.github/workflows/frontend-ci.yaml'  

permissions:
  id-token: write
  contents: read  

env:
  AWS_REGION: us-east-1
  ECR_REGISTRY: 317429619308.dkr.ecr.us-east-1.amazonaws.com
  ECR_REPOSITORY: frontend-react
  AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}

jobs:
  frontend-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Determine version bump type
        id: bump-type
        run: |
          COMMIT_MSG=$(git log -1 --pretty=%B)
          echo "Commit message: $COMMIT_MSG"
          
          if echo "$COMMIT_MSG" | grep -qiE "BREAKING CHANGE:|!:|MAJOR"; then
            echo "bump_type=major" >> $GITHUB_OUTPUT
            echo "Bump type: MAJOR"
            
          elif echo "$COMMIT_MSG" | grep -qiE "MINOR"; then
            echo "bump_type=minor" >> $GITHUB_OUTPUT
            echo "Bump type: MINOR"
            
          # default PATCH (feat:, fix:, chore:)
          else
            echo "bump_type=patch" >> $GITHUB_OUTPUT
            echo "Bump type: PATCH"
          fi

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Calculate next version from ECR
        id: version
        run: |
          echo "Querying ECR for existing versions..."
          
          ALL_TAGS=$(aws ecr describe-images \
            --repository-name ${{ env.ECR_REPOSITORY }} \
            --region ${{ env.AWS_REGION }} \
            --query 'imageDetails[].imageTags[]' \
            --output text 2>/dev/null || echo "")
            
          echo "All tags found in ECR: $ALL_TAGS"
          
          LATEST_VERSION=$(echo "$ALL_TAGS" | tr '\t' '\n' | grep -E '^[0-9]+\.[0-9]+\.[0-9]+$' | sort -V | tail -n 1)
          
          echo "Detected LATEST_VERSION from ECR: '$LATEST_VERSION'"
          
          if [[ -n "$LATEST_VERSION" ]]; then
            MAJOR=$(echo "$LATEST_VERSION" | cut -d'.' -f1)
            MINOR=$(echo "$LATEST_VERSION" | cut -d'.' -f2)
            PATCH=$(echo "$LATEST_VERSION" | cut -d'.' -f3)
            echo "Parsed -> MAJOR: $MAJOR, MINOR: $MINOR, PATCH: $PATCH"
          else
            MAJOR=0
            MINOR=0
            PATCH=0
            echo "No existing version found, starting from 0.0.0"
          fi
          
          BUMP_TYPE="${{ steps.bump-type.outputs.bump_type }}"
          echo "Applying bump type: $BUMP_TYPE"
          
          case "$BUMP_TYPE" in
            major)
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              ;;
            minor)
              MINOR=$((MINOR + 1))
              PATCH=0
              ;;
            patch)
              PATCH=$((PATCH + 1))
              ;;
          esac
          
          NEW_VERSION="${MAJOR}.${MINOR}.${PATCH}"
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "major=$MAJOR" >> $GITHUB_OUTPUT
          echo "minor=$MINOR" >> $GITHUB_OUTPUT
          echo "patch=$PATCH" >> $GITHUB_OUTPUT
          echo "major_minor=${MAJOR}.${MINOR}" >> $GITHUB_OUTPUT
          echo "✅ Final Calculated New version: $NEW_VERSION"

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and Push Docker Image
        run: |
          IMAGE_TAG="${{ steps.version.outputs.new_version }}"
          
          echo "Building Docker image with tag: $IMAGE_TAG for ECR..."
          docker build -t ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG .
          
          echo "Pushing Docker image to ECR..."
          docker push ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG
          
          # 同时给镜像打上 latest 标签并推送（方便其他环境或测试快速拉取）
          docker tag ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:$IMAGE_TAG ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:latest
          docker push ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:latest

      - name: Output build summary
        run: |
          echo "✅ React Frontend Build & Push completed successfully!"
          echo "Image: ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:${{ steps.version.outputs.new_version }}"
          echo "Version: ${{ steps.version.outputs.new_version }}"
          echo "Bump type: ${{ steps.bump-type.outputs.bump_type }}"
          echo "Commit: ${{ github.sha }}"